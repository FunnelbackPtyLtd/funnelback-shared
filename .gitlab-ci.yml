include:
  - project: funnelback-rnd/gitlab-templates
    ref: main
    file: all.yml
  - project: funnelback-rnd/gitlab-templates
    ref: main
    file: security.yml

default:
  image:
    name: docker-hub.squiz.net/funnelback-rnd/gitlab-ci-docker/centos7-jdk21:latest
    entrypoint: [""]
  tags:
    - general/small

cache:
  key: "$CI_JOB_NAME"
  paths:
    - .m2/repository

stages:
  - build
  - integration

variables:
  # `showDateTime` will show the passed time in milliseconds. You need to specify `--batch-mode` to make this work.
  MAVEN_OPTS: >-
    -Dhttps.protocols=TLSv1.2
    -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository
    -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN
    -Dorg.slf4j.simpleLogger.showDateTime=true
    -Djava.awt.headless=true
  # As of Maven 3.6.1, the use of `--no-transfer-progress` (or `-ntp`) suppresses downloads and uploads messages.
  MAVEN_CLI_OPTS: >-
    --batch-mode
    --errors
    --show-version
    --no-transfer-progress
    --update-snapshots

.artifacts: &artifacts
  artifacts:
    expire_in: 1 hr
    paths:
      - "**/target/*"
    reports:
      junit:
        - "**/target/surefire-reports/TEST-*.xml"
        - "**/target/failsafe-reports/TEST-*.xml"

# Compile `funnelback-shared` and execute unit tests
build:
  stage: build
  # Template-merged jobs may carry an explicit JDK11 image; pin build to JDK21.
  image:
    name: docker-hub.squiz.net/funnelback-rnd/gitlab-ci-docker/centos7-jdk21:latest
    entrypoint: [""]
  script:
    - mvn $MAVEN_CLI_OPTS -fail-at-end clean install
    - rm -rf .m2/repository/com/funnelback/
  <<: *artifacts

# Verify if changes from `funnelback-shared` affect/break released plugins
#verify_plugins:
#  stage: integration
#  variables:
#    FUNNELBACK_SHARED_BRANCH: $CI_COMMIT_BRANCH
#  needs:
#    - build
#  trigger:
#    project: funnelback-rnd/funnelback-plugins-mono-repo
#    branch: master
#    strategy: depend

# GitLab SpotBugs analyzer v4/v5 runs on older JDKs and fails for release 21 projects.
# Pinning to v6 enables JDK 21 support for SpotBugs SAST scans.
spotbugs-sast:
  image:
    name: registry.gitlab.com/security-products/spotbugs:6
  variables:
    SAST_ANALYZER_IMAGE_TAG: "6"
    SAST_JAVA_VERSION: "21"

