image: docker-hub.squiz.net/funnelback-rnd/gitlab-ci-docker:base-jdk11

build:
    script:
        - mvn -B -U -Dmaven.test.failure.ignore=false clean verify
    tags:
        - docker
        - shared

