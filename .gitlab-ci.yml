variables:
  # This will supress any download for dependencies and plugins or upload messages which would clutter the console log.
  # `showDateTime` will show the passed time in milliseconds. You need to specify `--batch-mode` to make this work.
  # Lifted from https://gitlab.com/gitlab-org/gitlab-ci-yml/blob/master/Maven.gitlab-ci.yml
  MAVEN_OPTS: "-Dhttps.protocols=TLSv1.2 -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN -Dorg.slf4j.simpleLogger.showDateTime=true -Djava.awt.headless=true"

cache:
    key: "$CI_JOB_NAME"
    paths:
        - .m2/repository

stages:
  - build
  - release

build:
    stage: build
    image: docker-hub.squiz.net/funnelback-rnd/gitlab-ci-docker:maven-central

    script:
        - mvn -B -U clean verify -Dgpg.passphrase=$GPGPASSPHRASE
        - rm -rf .m2/repository/com/funnelback/

    artifacts:
        expire_in: 1 hr
        paths:
            - target/*
        reports:
            junit:
                - "*/target/surefire-reports/TEST-*.xml"
                - "*/target/failsafe-reports/TEST-*.xml"

    tags:
        - docker
        - shared

release:
    stage: release
    when: manual
    only:
        - maven-central-pom-files
    # TODO - change that to master before this goes in!
    image: docker-hub.squiz.net/funnelback-rnd/gitlab-ci-docker:maven-central

    script:
        - mvn -B -U clean deploy -Dgpg.passphrase=$GPGPASSPHRASE
        - rm -rf .m2/repository/com/funnelback/

    artifacts:
        expire_in: 1 hr
        paths:
            - target/*
        reports:
            junit:
                - "*/target/surefire-reports/TEST-*.xml"
                - "*/target/failsafe-reports/TEST-*.xml"

    tags:
        - docker
        - shared
