triggerActions:
  -
    trigger: !AllQueryWords
      triggerWords:
        - uniquequery
    actions:
      actions:
        - !DisplayMessage
          message:
            additionalProperties: null
            category: no-category
            messageHtml: uniquemessage
        - !PromoteUrls
          urlsToPromote:
            - uniqueurl
