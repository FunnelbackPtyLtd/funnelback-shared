<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>${groupId}</groupId>
    <artifactId>${artifactId}</artifactId>
    <packaging>jar</packaging>
    <version>${version}</version>
    <name>Funnelback plugin</name>
    <dependencies>
        <dependency>
            <artifactId>funnelback-shared</artifactId>
            <groupId>com.funnelback</groupId>
            <version>${archetypeVersion}</version>
        </dependency>
        <!-- add your dependencies here -->
    </dependencies>
    <build>
        <sourceDirectory>${version}/src/main/java</sourceDirectory>
        <resources>
            <resource>
                <directory>${version}/src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <outputDirectory>${version}/libs</outputDirectory>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>