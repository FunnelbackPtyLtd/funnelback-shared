/**
 * <p>Provides classes representing the Curator system's configuration, including actions 
 * (what should happen) and triggers (when should it happen).</p>
 * 
 * @since 13.0
 */
package com.funnelback.publicui.search.model.curator.config;

